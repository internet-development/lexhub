# Database Configuration
DATABASE_URL=postgresql://lexhub:lexhub_password@localhost:5432/lexhub
# DATABASE_SSL can be: require | disable (optional)
# If omitted, SSL is required for non-local hosts.
DATABASE_SSL=

# Next.js Configuration
NODE_ENV=development
PORT=10000

# Tap Configuration
# Shared secret for webhook authentication (must match TAP_ADMIN_PASSWORD on TAP)
TAP_ADMIN_PASSWORD=
